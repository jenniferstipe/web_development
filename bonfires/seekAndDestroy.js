function destroyer(arr) {
  // Remove all the values

  function filter(values, match) {
    for (x=1;x < values.length; x++) {
       
    }
  }
  
  arr = arr.filter(2);
  return arr;
}



destroyer([1, 2, 3, 1, 2, 3], 2, 3);
